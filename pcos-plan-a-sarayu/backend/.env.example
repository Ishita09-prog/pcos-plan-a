# Postgres connection string used by app/database.py.
# Leave unset to fall back to a local SQLite file (fine for the demo / student build).
DATABASE_URL=postgresql+psycopg2://pcos:pcos@localhost:5432/pcos_plan_a
